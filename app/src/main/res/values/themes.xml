<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyAlarm" parent="android:Theme.Material.Light.NoActionBar">
        <item name="theme">@style/Base.Theme.MyAlarm</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyAlarm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">#33FFFFFF</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:background">#33FFFFFF</item>
        <item name="android:colorBackground">#33FFFFFF</item>
    </style>
</resources>